# OMEN SERVER GATEWAY 환경 변수 설정

# PostgreSQL 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=omen_gateway
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# 세션 보안 키 (복잡한 랜덤 문자열로 변경 필요)
SESSION_SECRET=your-very-secure-session-secret-here-change-this

# 애플리케이션 포트 (선택사항, DB에서 설정 가능)
PORT=7777

# 백업용 관리자 계정 (DB 연결 실패 시에만 사용)
BACKUP_ADMIN_PHONE=01034424668
BACKUP_ADMIN_PASSWORD=fallback-password